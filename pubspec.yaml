name: currency_app
description: Currency application.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get_it: ^6.0.0
  flutter_bloc: ^7.0.0
  equatable: ^2.0.0
  http: ^0.13.1
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
